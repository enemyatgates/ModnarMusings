<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ .Title }} | {{ .Site.Title }}</title>
  {{ if .IsHome }}<link rel="alternate" type="application/rss+xml" title="RSS" href="{{ .Site.BaseURL }}index.xml" />{{ end }}
  {{ block "head" . }}{{ end }}
</head>
<body>
  <header>
    <h1><a href="{{ .Site.BaseURL }}">{{ .Site.Title }}</a></h1>
  </header>

  <main>
    {{ block "main" . }}{{ end }}
  </main>

  <footer>
    <small>© {{ now.Format "2006" }} {{ .Site.Title }}</small>
  </footer>
</body>
</html>
